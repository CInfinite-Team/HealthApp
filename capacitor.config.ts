import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cinfinite.nexthealth.app',
  appName: 'NextHealthApp',
  webDir: 'out'
};

export default config;
